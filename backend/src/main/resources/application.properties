# ===============================
# SERVER
# ===============================
server.port=${PORT:8080}

# ===============================
# DATASOURCE (Railway / Env-based)
# ===============================
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

<<<<<<< HEAD
# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
=======

# ======================================================
# JPA / HIBERNATE (SAFE MODE)
# ======================================================
# Use validate instead of update to avoid schema changes
spring.jpa.hibernate.ddl-auto=validate

# MySQL dialect (for Hibernate 7.x)
>>>>>>> 3fe5072 (Add Dockerfile for Railway deployment)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# ===============================
# FLYWAY
# ===============================
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# ===============================
# WEBSOCKET
# ===============================
spring.websocket.path=/ws

# ===============================
# JWT
# ===============================
jwt.secret=${JWT_SECRET:WHITEBOARD_V2_SUPER_SECRET_SIGNING_KEY_2025_!@#$}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ===============================
# LOGGING
# ===============================
logging.level.root=INFO
<<<<<<< HEAD
=======
logging.level.org.springframework.web=INFO


# ======================================================
# JACKSON (JSON SAFETY)
# ======================================================

spring.jackson.time-zone=UTC


# ======================================================
# CORS (OPTIONAL ? SAFE DEFAULT)
# ======================================================
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
>>>>>>> 3fe5072 (Add Dockerfile for Railway deployment)
